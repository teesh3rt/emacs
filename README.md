# My .emacs.d
This is my .emacs.d. It aims to be modular so that its easy to add new features.
To add a new feature, simply make a new file in `plugs/` and start coding!

# Quick start
```
git clone https://github.com/teesh3rt/emacs ~/.emacs.d
```